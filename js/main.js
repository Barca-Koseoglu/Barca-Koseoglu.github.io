/**
 * Barca Koseoglu - Personal Website
 * Shared JS for navigation and interactions
 */

document.addEventListener('DOMContentLoaded', () => {
    // Navigation links are always visible side-by-side on all screen sizes
    // No mobile toggle needed
});
